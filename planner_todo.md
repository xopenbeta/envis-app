# TODO List:
1. 修改 `src-tauri/src/types/mod.rs`，增加 `Nasm` 类型。
2. 修改 `src-tauri/src/manager/builders/envvars.rs` 和 `metadata.rs`。
3. 创建 `src-tauri/src/manager/services/nasm/mod.rs`。
4. 创建 `src-tauri/src/manager/services/nasm/nasm.rs`。
5. 修改 `src-tauri/src/manager/services/mod.rs`。
